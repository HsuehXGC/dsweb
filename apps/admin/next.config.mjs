/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@ant-design/nextjs-registry', 'antd', '@ant-design/icons'],
};

export default nextConfig;
